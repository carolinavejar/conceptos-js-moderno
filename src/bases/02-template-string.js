// variables y constantes

const namee = "carolina";
const lastName = "vejar";

const fullName = `${namee} ${lastName}`;

console.log(fullName);

function getSaludo(name) {
    return `Hola ${name}`;
}

console.log(`Este es un texto ${getSaludo("caro")}`);